name: cicd

on:
  push:
    branches:
      - 'feature/*'
  pull_request: {}
#    types: [opened, reopened]

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Docker meta
        id: meta
        uses: docker/metadata-action@v4
        with:
          # list of Docker images to use as base name for tags
          images: |
            ghcr.io/${{ github.actor }}/${{ github.event.repository.name }}
          # generate Docker tags based on the following events/attributes
          tags: |
            type=sha,prefix=
            type=semver,pattern={{version}},prefix=
            type=semver,pattern={{major}}.{{minor}}.{{patch}},prefix=
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      -
        name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}       
      -
        name: Build and push
        uses: docker/build-push-action@v3
        with:
          push: false
          tags: ${{ steps.meta.outputs.tags }}
          context: "{{defaultContext}}:build"
#
      - name: Update deployment file
        run: |
           echo 0
#          TAG=sed -i 's|<IMAGE>|${{ steps.meta.outputs.tags }}|' $GITHUB_WORKSPACE/helm/values_dev.yml
#          git config --local user.name "github-actions"
#          git add $GITHUB_WORKSPACE/helm/values_dev.yml
#          git commit -m "Auto update at $(date +%Y-%m-%d-%H-%m)"
#          git push

